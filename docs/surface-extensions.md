# Surface extensions · Поверхневі розширення

**Owner 2026-09-10** (ecosystem): `.my ↔ .мій`, `.wsm ↔ .всм`, `.lisp ↔ .лісп`.

## Scope in `panca-vac`

| Area | Status |
|------|--------|
| Corpus / observations / alignments | Markdown + YAML — **not-applicable** |
| Globs / CI loaders for `.my`/`.wsm` | **None in this repo** |
| UTF-8 paths / Ukrainian filenames | Allowed; no filter rejects them |
| Fixtures `приклад.мій` etc. | **Not required** — no parser path |

Acceptance for ecosystem issue: Latin fixtures N/A; no code path to break.
Report: **not-applicable**, no extra code.

Issue: panca-vac#1
